%h2 Enter your Email
%p Bear with us, but we need an email to notify you and reset you of your password if you ever loose it.
= form_tag({:controller => "accounts", :action => "email"},{:class => "simple_form"}) do
  .input.string.email
    = email_field_tag :email, nil, :placeholder => t(:email_example), :class => "input string email"
  = submit_tag("Ok")
