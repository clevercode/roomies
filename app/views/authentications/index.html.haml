%h1 Sign In

- if @authentications
  - unless @authentications.empty?
    %p You can sign in to this account using:
    #authentications
    - for authentication in @authentications
      #authentication
        = image_tag "authbuttons/#{authentication.provider}_32.png", :size => "32x32"
        .provider #{authentication.provider.titleize}
        .uid #{authentication.uid}
        = link_to "X", authentication, :confirm => "Are you sure you want to remove this authentication option?", :method => :delete, :class => "remove"
- else
%p Sign in using one of these services  

%ul
  %li= link_to image_tag( 'authbuttons/twitter_64.png', :size => "64x64", :alt => "Twitter"), 'auth/twitter'
  %li= link_to image_tag( 'authbuttons/facebook_64.png', :size => "64x64", :alt => "Facebook"), 'auth/facebook'
  %li= link_to image_tag( 'authbuttons/google_64.png', :size => "64x64", :alt => "Google"), 'auth/google'
